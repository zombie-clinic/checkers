MoveRequest:
  type: object
  properties:
    gameId:
      type: string
    side:
      type: string
    move:
      type: string
    playerId:
      type: integer
      format: int64
State:
  type: object
  required:
    - dark
    - light
  properties:
    dark:
      type: array
      items:
        type: integer
        format: int32
    light:
      type: array
      items:
        type: integer
        format: int32
