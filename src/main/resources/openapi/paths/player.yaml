get:
  tags:
    - Player
  operationId: getPlayers
  responses:
    '200':
      description: Successful operation
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../components/responses.yaml#/PlayerResponse'